apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "letsencrypt.fullname" . }}-test-connection"
  labels:
    {{- include "letsencrypt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "letsencrypt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
