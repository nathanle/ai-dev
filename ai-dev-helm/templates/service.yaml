apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-dev.fullname" . }}
  labels:
    {{- include "ai-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-dev.selectorLabels" . | nindent 4 }}
